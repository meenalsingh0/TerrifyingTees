{
  "name": "terrifying-tees",
  "version": "1.0.0",
  "description": "Root workspace to run frontend and backend",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "envy-check": "^0.1.2"
  }
}
