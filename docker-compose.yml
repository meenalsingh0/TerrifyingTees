services:

  nginx:
    build:
      context: ./nginx

    container_name: terrifyingtees-nginx

    ports:
      - "80:80"

    depends_on:
      - frontend
      - backend

    networks:
      - terrifyingtees-network

  backend:
    build:
      context: ./backend

    container_name: terrifyingtees-backend

    env_file:
      - ./backend/.env

    expose:
      - "3001"
    
    networks:
      - terrifyingtees-network

  frontend:
    build:
      context: ./frontend

    container_name: terrifyingtees-frontend

    expose:
      - "3000"

    depends_on:
      - backend
    
    networks:
      - terrifyingtees-network

networks:
      - terrifyingtees-network